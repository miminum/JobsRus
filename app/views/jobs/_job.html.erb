<div>
  <h3><%= job.title %></h3>
  <h4><%= job.company.name %></h4>
  <p>
    <%= job.front_end ? 'front-end' : '' %>
    <%= job.back_end ? 'back-end' : '' %>
  </p>
  <p><%= job.description %></p>
</div>